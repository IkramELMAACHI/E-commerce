1.laravel new blog
2. npm install
3.composer install
4.composer require laravel/ui
5.php artisan help ui
6.


# demande help
php artisan help name-of-help  >> exemple migrate
--------------------------------------------------------------------------------------------
# creation controlleur  (app\Http\controllers)
php artisan make:controlleur name-of-controlleur
--------------------------------------------------------------------------------------------
# route avec un controlleur
Route::get('/index-link',name-of-controlleur@name-of-method)
---------------------------------------------------------------------------------------------
#création un model  (app\)
php artisan make:model Todos
----------------------------------------------------------------------------------------------
# créer une migration par exemple ajouter une nouvelle table
php artisan make:migration create_todos_table
---------------------------------------------------------------------------------------------
#regler le parametre pour la migration update laravel
add this in (app:providers/AppServiceProvider.php) >>
    use Illuminate\Support\Facades\Schema;

public function boot()
{
    Schema::defaultStringLength(191);
}
----------------------------------------------------------------------------------------------
#créer fake data (database\factories\nameFactory.php)
php artisan make:factory name_of_factory
    exemple:
     (dont forget to add in top of file nameFactory.php => use App\name-of-model)
    $factory->define(name-of-model::class, function (Faker $faker) {
    return [
        'title'=>$faker->sentence(3),
        'description'=>$faker->text(100),
        'completed'=>$faker->false
    ];
});
------------------------------------------------------------------------------------------------------
#define parametre de seed (seeds\nameSeeder.php)
php artisan make:seeder nameSeeder
 public function run()
    {
        factory(App\name-model::class, nbr-of-data)->create();
    }

#define les seed à envoyer dans ce fichier golobal (seeds\DatabaseSeeder.php)

 public function run()
    {
        $this->call(nameSeeder::class);
    }
}
et php artisan db:seed (activer l'envoie)
---------------------------------------------------------------------------------------
#créer model et migration meme temp
php artisan make:model name -m
---------------------------------------------------------------------------------------
#changer le nom d'une table base donnée 
ajouter cette ligne dans le model :
protected $table = 'my_todos';
---------------------------------------------------------------------------------------
#pour recuperr un tableau et le passer dans view
 $todos = \App\Todo::all();
        1.methode
        return view('todos',['todos'=>$todos]);
        2.methode
         return view('todos')->with('todos',$todos);
        3.methode
         return view('todos', compact('todos'));
---------------------------------------------------------------------------------------
#pour utiliser name-of-model::all() sans link
add  => use App\name-of-model in controller         
--------------------------------------------------------------------------------------
# passer un parametre dans un url 
Route::get('/index/{parametre}','nameController@functionName');
utiliser la fonction find pour recuper la base de donnée 
 functionName($todo){
     return view('dossier.single')->with('parametre',Model::find($todo));

 }

